---
description: Commit and push all changes to trigger a Cloudflare Pages deployment
---

1. Run `git status` to see what changed.
2. Run `git add .` to stage all changes.
3. Run `git commit -m "..."` with a clear, specific message summarizing what changed.
// turbo
4. Run `git push origin main`.
5. Confirm the push succeeded, and remind me that Cloudflare Pages will automatically build and deploy this to app.hitec.id within 1–3 minutes.
